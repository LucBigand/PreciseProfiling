"
Exception called when the interpreter does not find the primitive whose pragma is the same as that of the method currently simulated
"
Class {
	#name : #CHPrimitiveFail,
	#superclass : #Exception,
	#category : #'Champollion-Core'
}
